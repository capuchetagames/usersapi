apiVersion: v1
kind: Service
metadata:
  name: users-api
spec:
  selector:
    app: users-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
#      nodePort: 30080
#  type: NodePort
  type: ClusterIP
