apiVersion: v1
kind: Service
metadata:
  name: users-api
  namespace: fiapstore
  labels:
    app: users-api
spec:
  type: LoadBalancer
  selector:
    app: users-api
  ports:
    - name: http
      protocol: TCP
      port: 8080
      targetPort: 8080
